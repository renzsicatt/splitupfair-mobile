# SplitUpFair Mobile

Expo React Native client for iOS and Android.

1. Copy `.env.example` to `.env` and set the deployed Render API URL.
2. Run `npm install`.
3. Run `npm start`, then open it using Expo Go.
